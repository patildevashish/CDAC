//Write a program to find second largest number in an array.
#include<iostream>
using namespace std;
int main() {
    int n,i,j,k,arr;
    cout<<"Enter the size of your array: ";
    
    cin>>n;
    cout<<"\nEnter "<<n<<" elements:\t";
    while(i!=n)
    {
        cin>>arr[i];
        if (arr[i]>j && arr[i]<k){
            j = k;}
            }
            }
