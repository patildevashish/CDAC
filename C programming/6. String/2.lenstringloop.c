/* len of string using loop*/

#include<stdio.h>
int main()
{
    char str[20];
    int i;
    printf("\nEnter any string: ");
    scanf("%s",str);
    for ( i = 0; str[i]!='\0'; i++)
    {
        
    }
    printf("\nLenght of a string is:%d",i);
}